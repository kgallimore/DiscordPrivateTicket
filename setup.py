from setuptools import setup

setup(
   name='DiscordPrivateTicket',
   version='0.0.9',
   description='Creates private tickets, and grabs member lists',
   author='Trenchguns',
   install_requires=['discord.py', 'requests', 'gitpython', 'electrum']
)